// Uma função que recebe 2 numeros e retorne o maior dele

let num1 = 1
let num2 = 2

function retornarNumeroMaior (num1, num2) {
    return num1 > num2 ? num1 : num2;
}

const retornar2 = (x, y) => x > y ? x : y


console.log(retornar2(2, 6))
